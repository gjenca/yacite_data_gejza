
# for rec in records:
# if rec.myown:
 1. {{  rec.authors|authorsformat("{f. }{vv}{ll{ }}{, jj}")|join(", ") }}: ''{{rec.title}}'', {{rec.journal}}, '''{{rec.volume}}''' ({{rec.year}}) {{rec.startpage}}-{{rec.endpage}} {{rec.arxivurl}}
# endif
# endfor


